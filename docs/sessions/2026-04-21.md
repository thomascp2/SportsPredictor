# Session Log — 2026-04-21

## What Was Done

### Phase 3C — Dashboard Supabase Removal (4fb6a81d)
- Removed 4 `get_supabase()` call sites (fetch_szln_picks, _evaluate_line, fetch_hb_picks, fetch_hb_history) — all replaced with simple early returns
- Replaced "Supabase Sync" System tab section with live Turso row-count query (all 4 sports via HTTP pipeline)
- Deleted `get_supabase()` stub entirely
- Cleaned all Supabase references from docstrings and comments

### Phase 3D — Orchestrator Supabase Removal (4fb6a81d)
- `SUPABASE_SYNC_AVAILABLE = False` — removed the import try/except block entirely
- Prediction pipeline: Supabase block removed, Turso block promoted (no longer "redundant")
- Grading pipeline: extracted Turso grading sync from inside Supabase block, removed Supabase block
- pp-sync: removed early-return guard + entire Supabase sync block; Turso-only
- Removed `supabase_local_sync` imports from H+B and SZLN operations

### Phase 3E — Archive Supabase Code (4fb6a81d)
- Archived to _archive/: supabase_sync.py, backfill_smart_picks.py, backfill_supabase.py, purge_stale_rows.py
- sync/config.py: removed SUPABASE_URL / SUPABASE_SERVICE_ROLE_KEY vars
- sync/__init__.py, sync/turso_sync.py: updated docstrings

### Phase 3F — ai_edge Backfill (f82ec62a)
- Discovered supabase_sync write-back (which populated ai_edge in SQLite) was now archived — needed fix
- Ran `turso_migrate --fix-schema` → added ai_edge, ai_ev_2/3/4leg, game_time, odds_type to all 4 Turso DBs
- Computed ai_edge = (probability - breakeven) * 100 for 131,559 is_smart_pick=1 SQLite rows
- EV values computed with static leg_value fallbacks (standard=1.0, goblin=0.5, demon=1.5)
- Synced to Turso: NBA 52,458 rows (32 dates), NHL 18,606 (32 dates), MLB 25,285 (15 dates)
- Fixed orchestrator run_pp_sync: added SmartPickSelector write-back as step 2 between
  line fetch and Turso sync — going forward pp-sync populates ai_edge in SQLite first

### Roadmap Update (2a22e7f6)
- SIMPLIFY_PLAN.md: full status summary table, Phase 3 fully checked off, Phase 4 pre-deploy checklist added

## Current State

**Codebase is now fully Supabase-free.** All cloud writes go to Turso. Dashboard reads Turso with local SQLite fallback.

Commits this session: 4fb6a81d → f82ec62a → 54f54aef → d4970e52 → 2a22e7f6

## Up Next — Phase 4: Streamlit Cloud Deploy

Pre-deploy work (not yet done):
1. requirements.txt: remove `supabase>=2.0.0`, add `libsql-client`
2. Create `.streamlit/secrets.toml` template (redacted values, safe to commit)
3. Verify all shared/ module imports are try/except safe for Cloud runtime
4. Commit + push → deploy to share.streamlit.io

Deploy target: thomascp2/SportsPredictor → dashboards/cloud_dashboard.py
Secrets needed in Cloud UI:
- TURSO_NHL_URL, TURSO_NHL_TOKEN
- TURSO_NBA_URL, TURSO_NBA_TOKEN
- TURSO_MLB_URL, TURSO_MLB_TOKEN
- TURSO_GOLF_URL, TURSO_GOLF_TOKEN
