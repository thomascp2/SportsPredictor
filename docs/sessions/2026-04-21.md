# Session Log — 2026-04-21

## What Was Done (Morning Session)

### Phase 3C — Dashboard Supabase Removal (4fb6a81d)
- Removed 4 `get_supabase()` call sites (fetch_szln_picks, _evaluate_line, fetch_hb_picks, fetch_hb_history) — all replaced with simple early returns
- Replaced "Supabase Sync" System tab section with live Turso row-count query (all 4 sports via HTTP pipeline)
- Deleted `get_supabase()` stub entirely
- Cleaned all Supabase references from docstrings and comments

### Phase 3D — Orchestrator Supabase Removal (4fb6a81d)
- `SUPABASE_SYNC_AVAILABLE = False` — removed the import try/except block entirely
- Prediction pipeline: Supabase block removed, Turso block promoted
- Grading pipeline: extracted Turso grading sync, removed Supabase block
- pp-sync: removed Supabase block entirely; Turso-only
- Removed `supabase_local_sync` imports from H+B and SZLN operations

### Phase 3E–F — Archive + ai_edge Backfill
- Archived supabase_sync.py and related scripts to _archive/
- Ran ai_edge backfill: NBA 52K, NHL 18K, MLB 25K rows synced to Turso
- Fixed orchestrator run_pp_sync: SmartPickSelector write-back now happens before Turso sync

---

## What Was Done (Evening Session — Phase 4: Streamlit Cloud Deploy)

### Deploy Setup
- `requirements.txt`: removed `supabase>=2.0.0`, added `libsql-client>=0.3.0` + `openai>=1.0.0`
- Added `_bootstrap_secrets()` at dashboard startup — loads `st.secrets` → `os.environ` on Cloud, falls back to `.env` locally
- `.streamlit/secrets.toml.example`: committable template
- Repo made **public** on GitHub (required for Streamlit Community Cloud free tier)
- 9 secrets added to Cloud UI: TURSO x4 pairs + XAI_API_KEY

### Bugs Fixed During Cloud Testing
1. **UTC date bug**: Cloud server is UTC, defaulted to wrong date. Fixed with `_today_cst()` using `zoneinfo America/Chicago`.
2. **Game lines blank**: `fetch_game_predictions` used `libsql_client` async. Replaced with `_turso_request` HTTP.
3. **Performance tab blank**: `fetch_pnl_local` + `fetch_recent_results` were local-SQLite-only. Added Turso HTTP primary path.
4. **Wrong column name**: `p.confidence_tier` → `p.ai_tier` in Turso query.
5. **`st.stop()` in System tab**: Replaced with conditional guard — `st.stop()` was potentially interfering with tab rendering.
6. **Missed Turso sync**: game_predictions for 4/19–4/21 not in Turso. Manually synced all sports.
7. **MLB predictions**: 3151 rows synced to Turso for 4/21.

### Verified Turso Connections (all live)
- NHL: 73K predictions | NBA: 212K predictions | MLB + Golf: responding

### Working on Cloud
- Top Plays tab (NHL + NBA picks)
- NHL player props tab
- NBA player props tab
- Game Lines (NHL, NBA, MLB) — after manual sync
- Neutral messages for StatBot, System tab

### Still Pending Verification (after b45b7acc redeploy)
- Top Plays: was empty — should fix after st.stop() removal
- MLB player props: 3151 rows synced, verify props tab now shows
- Performance tab: Turso query fixed, verify P&L data appears

### Known Permanent Cloud Limitations
- MLB ML model comparison: requires local DuckDB — shows "not found" message
- StatBot: neutral info message (XAI key not exposed publicly — intentional)
- System tab: "local only" info message, health panels show "No data"

### Orchestrator Sync Coverage (confirmed automatic)
All sports sync to Turso after: `run_predictions`, `run_pp_sync`, `run_game_predictions`, `run_grading`. No manual intervention needed going forward.

### Commits This Session
- `e9138d3a` Phase 4 prep: requirements, secrets bootstrap, secrets.toml.example
- `4429f87f` Fix UTC date bug (_today_cst)
- `bc5a16ba` Route all data fetchers to Turso HTTP
- `0c56fa98` Fix performance tab: confidence_tier → ai_tier
- `b45b7acc` Remove st.stop() from System tab

---

## Handoff Prompt


```
Picking up from Phase 4 (Streamlit Cloud deploy) — live at share.streamlit.io.
Repo: thomascp2/SportsPredictor, branch master, file dashboards/cloud_dashboard.py.
Last commit: 698ec996

REMAINING BUGS TO FIX:

1. NBA Top Plays tab — not populating. NBA player props tab DOES work (same Turso data).
   Difference: Top Plays calls fetch_picks with tier_filter=[] (unhashable empty list).
   Fix: change to tier_filter=() in Top Plays call (~line 1770). Falsy tuple = no filtering, but hashable for @st.cache_data.

2. MLB player props tab — 231 smart picks in Turso but tab blank.
   Most likely: MLB Turso wakes slowly. Timeout bumped 10s->25s in 698ec996 — verify after redeploy resolves it.
   If still blank: check _turso_picks_http logs for MLB errors.

3. NBA game time — was missing. Fixed in 698ec996 (added game_time to SELECT). Verify after redeploy.

WORKING: Top Plays NHL, NHL props, NBA props, Game Lines all sports, StatBot/System/Perf neutral messages.
PERMANENT LIMITATIONS: MLB ML model (local DuckDB), StatBot (XAI key not public), System tab (local only).

BIG PICTURE: Full revamp still in progress. User has big ML model plans for Oct 2026 retrains.
After Cloud is stable, discuss next steps — user wants to move fast.
```
