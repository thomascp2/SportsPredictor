# Session Log — 2026-04-06 (Evening)

## Summary

Continuation from morning session. Completed 6 priority items from the project_session_2026-04-06b.md handoff.

---

## Work Completed

### 1. MLB Game Lines Audit — PASS
- `mlb/scripts/team_stats_collector.py` already had `SEASON_START_DATE = "2026-03-27"` (Opening Day)
- All `game_lines` probabilities in realistic range (38–66%)
- `home_moneyline` populated from DraftKings for all games
- No action needed.

### 2. Golf `is_smart_pick` / `ai_tier` Column Check — PASS
- Both Golf and MLB `predictions` tables already have both columns.
- No action needed.

### 3. Player Name Encoding Fix at Write Time
**Problem:** NHL API returns diacritics (Stützle, Slafkovský, etc.), NBA Stats API returns names like Dončić, Jokić. These were stored in SQLite as-is, causing:
- Mismatches in PrizePicks matching (Stutzle vs Stützle)
- Windows cp1252 encoding errors on print/log
- Inconsistent Turso smart_picks UPDATE keys

**Fix applied:**
- Added `_normalize_name()` (unicodedata NFD + strip Mn) as static method in:
  - `nhl/scripts/statistical_predictions_v2.py` — applied in `_save_prediction`
  - `nba/scripts/generate_predictions_daily.py` — applied at start of `_save_prediction`
  - `nba/scripts/generate_predictions_daily_V6.py` — same
- Added `_normalize_name()` to `nhl/scripts/v2_auto_grade_yesterday_v3_RELIABLE.py` — applied when iterating `player_stats_by_team` before writing to `player_game_logs`
- **One-time DB cleanup** (Python script, not committed):
  - NHL `predictions`: 1,163 rows normalized, `prediction_outcomes`: 1,085 rows
  - NBA `predictions`: 2,397 rows normalized, 1,649 duplicate rows deleted (ASCII + diacritic duplicates for same game/prop/line)
  - NBA `prediction_outcomes`: 2,908 rows normalized
  - NHL + NBA `player_game_logs`: 431 + 454 rows normalized
  - Final state: 0 non-ASCII player names in any table

### 4. StatBot on Streamlit Dashboard
**Added "StatBot" tab to `dashboards/cloud_dashboard.py`:**
- New 9th tab between Golf and System
- Chat UI using `st.chat_input` + `st.chat_message`
- Session history via `st.session_state.statbot_history`
- Calls Grok-3-mini via xAI API (same schema context as `scripts/stat_bot.py`)
- Auto-loads XAI_API_KEY from `.env` if not in environment
- Displays: AI note (plain English explanation) + SQL code block + dataframe results
- Graceful degradation: shows warning if XAI_API_KEY missing
- "Clear history" button to reset chat

### 5. NHL Grok H&B Tab Restore
**Problem:** `daily_hits_blocks.py` hadn't run since 2026-03-29 (8-day gap).
**Root cause:** Script requires `XAI_API_KEY` from environment — only set by `start_orchestrator.bat`, not available standalone.

**Fix:** Added `.env` auto-loader to `nhl/scripts/daily_hits_blocks.py` (same pattern as `scripts/stat_bot.py`). Script now loads all keys from `.env` at startup via `os.environ.setdefault()`.

**Generated:** Apr 6 picks — 8 floor plays (hits/blocked shots) via Grok-3, saved to `nhl/database/hits_blocks.db`. Dashboard H&B tab now shows current data.

### 6. MLB Season Props Tab Restyle (match Golf layout)
Changed `dashboards/cloud_dashboard.py` MLB Season Props tab to match Golf's visual style:
- Filter row: 4 → 5 columns (`[2, 1, 1, 1, 1]` matching Golf's `[2, 1, 1, 1, 1]`)
- All selectboxes: `label_visibility="visible"` → `label_visibility="collapsed"`
- Spacer before Refresh: `<div style="height:27px">` → `<br>`
- Sort control moved from inner Rankings sub-tab to outer filter row (accessible across all sub-tabs)

---

## Files Changed

| File | Change |
|------|--------|
| `nhl/scripts/statistical_predictions_v2.py` | `_normalize_name()` + apply in `_save_prediction` |
| `nba/scripts/generate_predictions_daily.py` | `_normalize_name()` + apply in `_save_prediction` |
| `nba/scripts/generate_predictions_daily_V6.py` | Same |
| `nhl/scripts/v2_auto_grade_yesterday_v3_RELIABLE.py` | `_normalize_name()` + apply in grading loop |
| `nhl/scripts/daily_hits_blocks.py` | `.env` auto-loader at startup |
| `nhl/scripts/team_stats_collector.py` | `SEASON_START_DATE=2025-10-01` + season filter (uncommitted from prior session, now committed) |
| `nhl/database/elo_ratings_nhl.json` | Updated ELO (uncommitted from prior session, now committed) |
| `dashboards/cloud_dashboard.py` | StatBot tab + MLB restyle |
| `mobile/src/services/api.ts` | Turso + Supabase fallbacks (from prior session, now committed) |
| `orchestrator.py` | MLB pp-sync scheduling (from prior session, now committed) |

**Commit:** `21d4f78` — feat(apr-06b): player name normalization, StatBot dashboard, H&B restore

---

## Architecture Notes

### Player Name Normalization — Policy (post-session)
- **Write time**: All prediction scripts normalize before SQLite INSERT
- **Grading time**: NHL grading normalizes before `player_game_logs` INSERT
- **Turso sync**: `sync_smart_picks` already handles both exact + normalized via two-pass; now rarely needed since SQLite is clean
- **Supabase sync**: `_normalize_name()` already applied at sync time (unchanged)
- **PrizePicks matching**: `smart_pick_selector.py` `_strip_diacritics()` remains as belt-and-suspenders

### StatBot Dashboard Access
- Tab: cloud_dashboard.py → "StatBot" (8th tab)
- Databases: NBA, NHL, MLB SQLite (local reads only)
- API: XAI Grok-3-mini (loads from .env)
- No data written — read-only queries only

---

## Open Items (carried forward)

| # | Item | Priority |
|---|------|----------|
| M4 | `production_predictor.py` — no degeneracy check on model load | Low |
| P4 | Mobile app has no data freshness timestamp | Low |
| P5 | NBA tier distribution nearly all T1-ELITE | Low |
| P6 | Orchestrator no catch-up logic after downtime | Low |
| — | NHL H&B gap Mar 30 – Apr 5 (missing picks) | Low — end of season |
| — | Auth setup (Google/Apple OAuth) — blocks device testing | Medium |
