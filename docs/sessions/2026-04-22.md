# Session Log — 2026-04-22

## Commits
- `e0bf082b` — Add ML v2 pipeline: BMA, MAB, KS drift detector, market edge
- `0941d023` — Fix UNDER demon/goblin picks violating PrizePicks rules

---

## ML v2 Pipeline (Phases 1–4 + 6) — COMPLETE

Five new files built and tested:

| File | Purpose |
|------|---------|
| `shared/market_odds_client.py` | True edge = model_prob - market_implied. Wraps Odds API. Works on free tier (game totals); player props light up on paid plan ($30/mo) |
| `ml_training/mab_weighting.py` | Thompson Sampling MAB — replaces hardcoded 60/40 blend. Persisted to `ml_training/mab_state/` |
| `ml_training/drift_detector.py` | KS test + SHAP + Discord alert. Runs post-grading |
| `ml_training/production_predictor.py` | `predict_bma()` added — BMA with std, CI, model_confidence, per-component probs |
| `ml_training/setup_ml_v2_schema.py` | `ml_v2_predictions` table created in all 3 sport DBs |

**Phase 5 (teammate On/Off features) still pending** — needs BALLDONTLIE API key registered at balldontlie.io, then added to `.env` as `BALLDONTLIE_API_KEY`.

---

## Bug Fix: UNDER demon/goblin picks (critical)

**Root cause:** `smart_pick_selector.py` correctly forces `prediction='OVER'` for demon/goblin lines in memory (line 736), but the pp-sync write-back in `orchestrator.py` and `turso_sync.py` never persisted the corrected direction. Original model `prediction='UNDER'` stayed in SQLite and flowed to Turso unchanged.

**Fix:**
- `orchestrator.py` line ~2563: added `prediction=pick.prediction` to SQLite UPDATE
- `turso_sync.py`: added `prediction` to SELECT + UPDATE; added safety-net enforcement (demon/goblin → OVER) at sync time
- Re-ran `python -m sync.turso_sync --sport nba --operation smart-picks --date 2026-04-22` to fix today's bad rows

**Prevention:** The turso_sync safety net now enforces the rule even if orchestrator somehow passes the wrong direction.

---

## Next Session

1. Register BALLDONTLIE free API key → add to `.env` → build `shared/teammate_features.py` (Phase 5)
2. Wire `ml-v2` operation into orchestrator (additive, reads from `ml_v2_predictions`)
3. Consider upgrading Odds API to paid plan ($30/mo) to unlock `true_edge` for player props
