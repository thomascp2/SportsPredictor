# PEGASUS requirements — inherits base from root requirements.txt
# Install from root first: pip install -r ../requirements.txt
# Then install extras below if needed

# Data processing (already in root)
pandas>=2.0.0
numpy>=1.24.0

# ML (already in root)
scikit-learn>=1.3.0
xgboost>=2.0.0

# DuckDB for mlb_feature_store reads
duckdb>=0.9.0

# Dashboard (already in root)
streamlit>=1.28.0
plotly>=5.17.0

# Turso sync
libsql-client>=0.3.0

# API (Phase 7+)
fastapi>=0.104.0
uvicorn>=0.24.0
